HOST=0.0.0.0
PORT=1337
APP_KEYS="toBeModified1,toBeModified2"
API_TOKEN_SALT=tobemodified
ADMIN_JWT_SECRET=tobemodified
TRANSFER_TOKEN_SALT=tobemodified
JWT_SECRET=tobemodified
ENCRYPTION_KEY=tobemodified

# Content seed (location pages + blogs). Default off.
# Set true for one controlled boot to insert missing seed JSON, then set false again.
# Create-only by default (no overwrite). See src/index.ts.
RUN_CONTENT_SEED=true
